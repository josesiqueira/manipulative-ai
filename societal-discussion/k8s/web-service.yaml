apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: bilingual-chatbot-experiment
  labels:
    app: web
spec:
  selector:
    app: web
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
