apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: api
  namespace: bilingual-chatbot-experiment
spec:
  host: api-bilingual-chatbot-experiment.2.rahtiapp.fi
  to:
    kind: Service
    name: api
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
